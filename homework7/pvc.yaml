apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: slava-pv-claim
spec:
  storageClassName: standard (default)
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi